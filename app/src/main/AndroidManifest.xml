<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  package="com.example.android.assignment3_pac">

  <application
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/AppTheme">
    <activity android:name=".Login">
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>

        <category android:name="android.intent.category.LAUNCHER"/>
      </intent-filter>
    </activity>
    <activity
      android:label="@string/title_activity_user_home"
      android:name=".UserHome"
      android:theme="@style/AppTheme.NoActionBar"/>
    <activity
      android:label="@string/title_activity_admin_home"
      android:name=".AdminHome"
      android:theme="@style/AppTheme.NoActionBar"/>
    <activity android:name=".CameraController"/>
    <activity
      android:label="@string/title_activity_camera_viewer"
      android:name=".CameraViewer"
      android:theme="@style/AppTheme.NoActionBar"/>
    <activity android:name=".LightbulbController"/>
    <activity android:name=".SmartPlugController"/>
    <activity android:name=".ThermostatController"/>
    <activity android:name=".ThermostatViewer"/>
    <activity android:name=".ManageUsers">
    </activity>
    <activity android:name=".ManageDevices">
    </activity>
    <activity android:name=".AddDevice">
    </activity>
  </application>

</manifest>